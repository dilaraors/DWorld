import { Base } from './base.model';

export class Register {
    base: Base;
    name: string;
    surname: string;
    userName: string;
    email: string;
    password: string;
    confirmPassword: string;
  }