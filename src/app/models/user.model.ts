import { Base } from "./base.model";

export class User {
    base: Base;
    name: string;
    surname: string;
    userName: string;
    email: string;
    password: string;
    profileImageURL: string;
  }